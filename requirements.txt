# Core runs on the Python standard library only (sqlite3, csv, datetime).
# These are needed only to go live with real providers, or to run the tests.
anthropic>=0.40      # real LLM hand (llm.RealLLM); fake is used without it
requests>=2.31       # real email hand (channels.PostmarkEmail); fake is used without it
pytest>=8.0          # test suite
